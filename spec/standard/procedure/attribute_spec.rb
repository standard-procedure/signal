# frozen_string_literal: true

RSpec.describe Standard::Procedure::Attribute do
  it "has a version number" do
    expect(Standard::Procedure::Attribute::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
